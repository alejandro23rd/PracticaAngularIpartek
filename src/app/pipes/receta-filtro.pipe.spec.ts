import { RecetaFiltroPipe } from './receta.pipe';

describe('RecetaFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new RecetaFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
